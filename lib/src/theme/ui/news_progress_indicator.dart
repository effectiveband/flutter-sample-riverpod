import 'package:flutter/material.dart';

class NewsProgressIndicator extends StatelessWidget {
  const NewsProgressIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
